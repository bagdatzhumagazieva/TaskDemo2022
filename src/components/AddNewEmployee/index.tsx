import React from "react";
import { Dialog } from "@mui/material"

interface IProps {
    open: boolean;
}

export const AddNewEmployee = ({open}:IProps) => {

    return (
        <Dialog open={open}>
            cd
        </Dialog>
    )
}
